# ProGrapple — Decision Log

Append-only record of decisions that shaped the project. Newest last. See
`CONVENTIONS.md` for the rules that do not change per feature, and
`ARCHITECTURE.md` for the design of the system itself.
