import './lib/sentry.js';

// Placeholder entry point. This scaffold renders nothing but the app name;
// features arrive in later tasks. Sentry is imported for its init side effect
// and is a silent no-op when no DSN is configured (local dev stays quiet).
document.querySelector('#app').textContent = 'ProGrapple';
